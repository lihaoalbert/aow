<h1>Product</h1>

<p>
<b>Name</b><br>
<%= @product.name %>
</p>

<p>
<b>Company</b><br>
<%= @product.company_id %>
</p>

<p>
<b>Tags</b><br />
<%= @product.tag_list %>
</p>

<h3>Custom Field</h3>
<% @product.field_groups.each do |fg| %>
  <% if fg.fields.present? %>
    <h5><%= fg.label %></h5>
    <% fg.fields.each do |field| %>
      <p>
      <b><%= field.label %></b><br />
      <%= @product.send(field.name) %>
      </p>
    <% end %>
  <% end %>
<% end %>

<div class="form-actions">
  <%= link_to 'Back', products_path, :class => 'btn'  %>
  <%= link_to 'Edit', edit_product_path(@product), :class => 'btn' %>
  <%= link_to 'Delete', product_path(@product), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-danger' %>
</div>
